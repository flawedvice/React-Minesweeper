

interface BoardProps {
    rows: number
    bombs: number
}

const Board = (props: BoardProps) => {
    return (
        <div>
            <h1>Board</h1>
        </div>
    );
};

export default Board;